一、模拟盘主流程
1、链接币安模拟环境
2、启动时，连续读取当前时间向前50天1分钟k线数据，舍弃掉最后一条未完成k线
3、计算多周期指标：

- MACD: 15m, 1h, 4h, 1d
- KDJ: 15m, 1h, 4h
- Volume MA: 1m重采样后的 MA5, MA10 (可选)
- 滚动均值 (Rolling Means): 针对 MACD 的 dif/dea/hist 计算前向均值
4、循环获取最新1分钟k线，更新上述多周期指标
5、执行策略信号判断 (V5.0逻辑)，生成开仓/平仓信号
6、执行交易逻辑（开仓、挂单、撤单、止盈止损）
7、每次交易逻辑执行完后，记录日志到数据库
8、每隔10分钟，保存当前模拟盘状态到文件，以便重启时继续
9、每次重启时，读取保存的状态文件，继续模拟盘流程
10、模拟盘日志，订单，系统运营仓鼠都可通过web界面查看，支持按时间段
11、模拟盘支持多版本切换，当前版本V5.0，后续版本可在配置文件中指定
12、模拟盘可以监控收益，可以暂停交易，调整参数等

二、V5.0 策略详细逻辑
1、核心指标

- **MACD**: 关注 DIF, DEA, HIST 在 15m, 1h, 4h, 1d 四个周期的数值。
- **KDJ**: 关注 J 值在 15m, 1h, 4h 的数值。
- **均值回归**: 对比当前指标值与过去 N 个周期的均值 (Means)。
- **价格波动**: 监控短期价格变化率，防止插针/剧烈波动时开仓。

2、开仓条件 (多头示例，空头反之)

- **阈值检查 (T0 Limits)**:
  - 15m, 1h, 4h, 1d 的 HIST 和 DIF 必须在配置的 [MIN, MAX] 范围内。
  - 过滤掉指标过高（已涨多）或过低（动能不足）的情况。
- **均值回归检查 (Means Checks)**:
  - 判断当前指标值 (HIST/DIF/DEA) 是否处于相对低位（多头）或高位（空头）。
  - 逻辑：`当前值 - 偏离阈值 < 历史均值` (多头)。
- **KDJ 过滤**:
  - J_15m, J_1h, J_4h 必须小于设定上限 (多头) 或大于设定下限 (空头)，防止在超买/超卖区逆势开仓。
- **价格防抖**:
  - 1分钟价格变化幅度不能超过 `M_PRICE_CHANGE`。
  - 过去 N 分钟内的价格极值波动不能超过设定比例 (`PRICE_CHANGE_LIMIT`)。
- **成交量过滤 (可选)**:
  - 若启用 `ENABLE_MA5_MA10`，需满足 `Vol_MA5 >= Vol_MA10`。

3、平仓与风控逻辑

- **止损 (Stop Loss)**:
  - 固定点数止损 (`STOP_LOSS_POINTS`)。
  - 触发后进入冷静期 (`STOP_LOSS_HOLD_TIME`)，期间不再开同向仓位。
- **分级止盈 (Tiered Take Profit)**:
  - 设定多个止盈层级 (`TP_LEVELS`)，价格达到层级后平掉部分仓位 (`TP_RATIO_PER_LEVEL`)。
  - 记录已触发的最高/最低价格 (`tp_hit_value`)。
- **止盈回撤 (Drawdown Close)**:
  - 仅在触发过至少一次分级止盈后激活。
  - 当价格从最高点(`tp_hit_value`)回撤超过 `DRAWDOWN_POINTS` 时，清空剩余仓位。
- **超时平仓 (Timeout)**:
  - 持仓超过 `CLOSE_TIME_MINUTES` 且未触发止盈时，强制平仓。

三、交易执行与订单管理
1、是否有持仓
  有持仓 -> 进入订单检测流程
  无持仓 -> 判断开仓条件 -> 满足则开仓并记录日志

2、订单检测与挂单逻辑
    - **开仓**:
      - 信号触发后，计算价格与数量。
      - 挂限价单 (Maker) 或 市价单 (Taker)。
      - 若挂单超过 3分钟 未成交，取消订单，重新评估。
    - **止损**:
      - 开仓成交后，立即在本地或服务端维护止损触发价。
      - 价格触及止损线，立即市价全平。
    - **止盈**:
      - 开仓成交后，根据分级止盈列表维护逻辑。
      - 价格触及某一级止盈，平掉对应比例仓位。
    - **止盈回撤**:
      - 触发分级止盈后，监控价格回撤。
      - 若价格回撤满足条件，挂单平掉剩余所有仓位。
      - 若挂单超时未成交，重新计算价格挂单。

3、日志记录
    - 详细记录开仓信号来源、触发时的指标数值。
    - 记录每一笔成交（开仓、止盈、止损、回撤、超时）的 价格、数量、手续费、盈亏。

四、订单类型
1、开仓订单：限价单(Maker) 优先，超时或配置为 Taker 则转市价。
2、止损订单：触发式市价单。
3、止盈订单：限价单/触发单。
4、止盈回撤订单：触发式平仓单。

五、异常处理与细节优化 (建议)
1、滑点处理 (Slippage)

- **现象**: 实际成交价与信号价不一致。
- **处理**:
  - **止盈止损基准**: 必须基于**实际成交均价 (Average Filled Price)** 计算 TP/SL，而非信号触发时的市场价。
  - **容忍度**: 市价单 (Taker) 设置价格保护范围（如当前价 ±1%），超过范围不成交，防止极端行情下的巨大滑点。
  - **统计**: 在日志中记录 `预期价格` vs `实际成交价` 的偏差，用于评估策略在当前流动性下的表现。

2、部分成交 (Partial Fills)

- **场景**: 挂单仅成交了一部分，价格随即反转。
- **策略**:
  - 设定超时时间 (如 3分钟)。
  - 超时后，若剩余部分小于最小下单数量，直接撤单。
  - 若剩余部分较大，可选择撤单并按当前市价补齐 (转Taker)，或仅保留已成交部分作为底仓运行。
  - **关键**: 无论成交多少，止盈止损单的数量必须与**当前实际持仓量**实时同步。

3、网络与数据异常

- **断线重连**: 建立心跳机制，若超过 1分钟 未收到 WebSocket 推送，触发 REST API 轮询或重连 WebSocket。
- **数据补全**: 重连成功后，必须检查断线期间的 K线数据，如有缺失需通过 REST API 补全，重新计算指标，防止信号丢失或错误触发。
- **API 限制**: 严格遵守币安 API 频率限制 (Rate Limits)，在代码中加入限流等待逻辑 (Rate Limiter)。

4、资金与持仓同步

- **启动检查**: 每次启动/重启时，首先调用 API 获取交易所实际持仓和余额。
- **强制同步**: 若本地记录的持仓与交易所不一致，以**交易所数据为准**，并发出告警日志。

六、模拟盘与回测、实盘的差异分析
1、模拟盘 vs 回测 (Backtest)

- **时间维度**: 回测是“上帝视角”加速运行，使用已完成的历史数据；模拟盘是“当下视角”实时运行，面临未来的不确定性。
- **数据特性**:
  - 回测数据通常是清洗过的标准 K 线。
  - 模拟盘面临 **数据延迟 (Latency)**、**丢包**、**K线未闭合时的信号闪烁 (Repainting)**。
  - **完善建议**: 必须严格实现 **K 线闭合检测机制**（例如：等待下一根 K 线的第一笔 tick 到达，或本地时间戳确认），确保信号固定后再执行，防止“信号消失”。
- **状态持久化**: 回测状态存在内存中；模拟盘必须将持仓、订单、累计盈亏实时写入数据库，以应对服务器重启或崩溃。

2、模拟盘 vs 实盘 (Live Trading)

- **是否完全一致？**: **否，存在关键差异**。
- **市场冲击 (Market Impact)**: 模拟盘订单是“虚拟”的，不会消耗盘口流动性；实盘大额订单会吃掉深度，导致滑点增加。
- **撮合机制**:
  - 模拟盘通常假设“价格触碰即成交”或“穿越即成交”。
  - 实盘遵循严格的 **价格优先、时间优先**，且受限于盘口深度，可能出现“触价不成交” (Limit Order not filled)。
- **资金费率 (Funding Rate)**: 模拟盘容易忽略此项；实盘永续合约每 8 小时结算一次资金费，长期持仓对收益影响巨大。
- **环境差异**:
  - 若连接 **Binance Testnet**：其价格走势、深度与主网 (Mainnet) 完全不同，策略表现可能失真。
  - **完善建议**: 推荐 **"Mainnet Paper Trading"** 模式 —— 订阅 **币安实盘 (Mainnet)** 的实时行情数据，但在本地逻辑中模拟账户和订单撮合。这样能验证策略在真实市场波动下的表现。

3、进一步完善方向

- **信号一致性**: 确保模拟盘的指标计算库 (如 TA-Lib 或 pandas rolling) 与回测代码完全一致，避免因精度差异导致信号不同步。
- **延迟监控**: 在日志中记录 `Signal_Time` (K线收盘) vs `Order_Time` (下单) vs `Fill_Time` (成交) 的时间差，评估系统延迟。
- **费率模拟**: 在模拟盘结算中，严格扣除 手续费 (Maker/Taker) 和 资金费率。

七、开发环境

- 语言: Python 3.13
- 依赖: pandas, sqlalchemy, ccxt (或 binance-futures-connector), flask (Web UI)
